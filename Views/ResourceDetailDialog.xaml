<Window x:Class="AccessibleTaskManager.Views.ResourceDetailDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Technical Hardware Details"
        Height="480" Width="580"
        MinHeight="360" MinWidth="440"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResizeWithGrip"
        ShowInTaskbar="False"
        Background="{DynamicResource WindowBackgroundBrush}"
        Foreground="{DynamicResource TextPrimaryBrush}"
        FontSize="14">

    <Window.Resources>
        <Style TargetType="ListBoxItem">
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="Margin" Value="0,2"/>
            <Setter Property="Background" Value="{DynamicResource SurfaceBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{DynamicResource SelectionBackgroundBrush}"/>
                    <Setter Property="Foreground" Value="{DynamicResource SelectionForegroundBrush}"/>
                    <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="txtTitle" Grid.Row="0" FontWeight="Bold" FontSize="16" Foreground="{DynamicResource AccentBrush}"
                   Text="Technical Hardware Details"
                   AutomationProperties.Name="{Binding Text, RelativeSource={RelativeSource Self}}"/>

        <TextBlock Grid.Row="1" Margin="0,4,0,8" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12"
                   Text="Use Up and Down Arrow keys to review technical specifications. Press Escape to close."/>

        <ListBox x:Name="lstDetails" Grid.Row="2" Margin="0,4,0,12"
                 Background="{DynamicResource WindowBackgroundBrush}" BorderThickness="0"
                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 KeyDown="LstDetails_KeyDown"
                 AutomationProperties.Name="Technical Specifications List">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}" TextWrapping="Wrap" AutomationProperties.Name="{Binding}"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <DockPanel Grid.Row="3">
            <Button x:Name="btnFlushMemory" Content="_Flush Memory Cache (F)" Height="32" Padding="12,0" Margin="0,0,10,0"
                    Visibility="Collapsed" Click="BtnFlushMemory_Click"
                    Background="{DynamicResource AccentBrush}" Foreground="{DynamicResource SelectionForegroundBrush}"
                    BorderBrush="{DynamicResource AccentBrush}" FontWeight="SemiBold"
                    AutomationProperties.Name="Flush Standby and Working Set Memory Cache (Hotkey F)"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="btnCopy" Content="Copy to Clipboard" Width="140" Height="32" Margin="0,0,10,0"
                        Click="BtnCopy_Click"
                        Background="{DynamicResource SurfaceBackgroundBrush}" Foreground="{DynamicResource TextPrimaryBrush}"
                        BorderBrush="{DynamicResource BorderBrush}"
                        AutomationProperties.Name="Copy Technical Information to Clipboard"/>
                <Button x:Name="btnClose" Content="Close (Escape)" Width="120" Height="32"
                        IsCancel="True" Click="BtnClose_Click"
                        Background="{DynamicResource SurfaceBackgroundBrush}" Foreground="{DynamicResource TextPrimaryBrush}"
                        BorderBrush="{DynamicResource BorderBrush}"
                        AutomationProperties.Name="Close Dialog"/>
            </StackPanel>
        </DockPanel>
    </Grid>
</Window>
